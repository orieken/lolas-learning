# Playwright E2E configuration
# Boolean flags accept: 1|0|true|false|yes|no|on|off (case-insensitive)

# Run browsers headless (default true)
HEADLESS=true

# Record video: allowed values: on | retain-on-failure | on-first-retry | off
# If boolean-like true, defaults to on-first-retry.
RECORD_VIDEO=on-first-retry

# Screenshots: allowed values: on | only-on-failure | off
# If boolean-like true, defaults to only-on-failure.
SCREENSHOTS=only-on-failure

